join(int sumL | int sumR) returns (sum) locals (int sum) {
	   sum = sumL + sumR ;
}

sequential (int* s, int** sa, int n) returns (sum) locals (int i, int sum) {
	   mts = 0;
	   sum = 0;
	   i = 0;
	   for(i = 0; i <= n; i++) {
	   sum = sum + s[i] + sa[i][i+1];
	   }
	   sum = sum * n + 3;


}
